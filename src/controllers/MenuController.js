const { accountModels } = require("../../database");

class MenuController {

    /**
     * fetching ATS menus 
     * @param {*} data 
     * @param {*} auth 
     * @param {*} callback 
     */
    static getMenusAndActivities (data, auth, callback){
        try {
            const auth = {}
        } catch (error) {
            callback(error);
        }

    }
}

module.exports = MenuController;